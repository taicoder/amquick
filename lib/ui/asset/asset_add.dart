import 'package:amquick/all_export.dart';

class AssetAdd extends StatelessWidget {
  const AssetAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("AssetAdd"),
    );
  }
}
